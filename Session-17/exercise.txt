1 ) Πως μπορουμε να κανουμε ενα Request σε ενα εξωτερικο API χωρις να γνωριζει το ολοκληρο 
URL μας ? 

# Απαντηση : Μπορουμε να καλεσουμε το εξωτερικο αυτο API μεσω ενος proxy - api κατι σαν διασυνδεση 
αναμεσα στα δυο , ετσι το ενα δεν θα βλεπει απευθειας το αλλο.
Ιδανικο θα ηταν ενα Interface που θα λαμβανει 
απαντησεις και θα κανει ερωτησεις απο το ενα API στο αλλο χωρις τα δυο αυτα API να επικοινωνουν απευθειας.

2. Αν κάνω ενα GET request και ζητήσω ενα jpeg image, το response τι 
content-type πρεπει να εχει;

#Απαντηση : Θα  πρεπει να ειναι (image/jpeg)

3 )Πως μπορώ να κάνω ένα GET request και το περιεχόμενο του να 
κρατηθεί στην cache για 1 μέρα

#Απαντηση : Θα πρεπει να στο header να μπει το argument Cache Control και εκει να ζητηθει η διαρκεια σε second

4. Μπορώ να έχω μια εφαρμογή που κάνει μόνο GET requests;

#Απαντηση : ναι , πχ ενας απλος browser κανει μονο GET Requests

5. Μπορώ να έχω μια εφαρμογή που κάνει μόνο POST requests;

#Απαντηση : Ναι , πχ μια εφαρμογη που στελνει μονο δεδομενα σε ενα server και κανει πχ update ενα log με συμβαντα της ημερας.

6. Τι status code περιμένω από ένα API αν δεν έχω στείλει ένα 
υποχρεωτικό πεδίο;

#Απαντηση : Θα ειναι 400 bad request καθως δεν θα μπορει ο server να καταλαβει τι πρεπει να κανει.



7. Τι status code περιμένω από ένα API αν δεν υπάρχει επικοινωνία με τη 
βάση;

#Απαντηση : Error code 500 δηλαδη κατι μην αναμενομενο εγινε , ισως πχ η βαση ειναι offline και ο server δεν μπορει να επικοινωνησει

8. Μπορώ να κάνω Login με GET request;

#Απαντηση : ναι αλλα θα ειναι φανερο και το username και το password επανω στο Url ,
			αρα οχι καλη πρακτικη. 

9. Ποια είναι η διαφορά ανάμεσα στο PUT και στο POST;

10. Ποια μέθοδο θα πρέπει να χρησιμοποιήσω για να «ανεβάσω» ένα 
αρχείο